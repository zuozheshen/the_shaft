# AGENTS.md

## 项目

《The Shaft》是一款使用 Godot 4.7 制作的叙事型电梯操作员演示项目。

玩家不是探索大楼的英雄，而是一名在电梯控制舱内工作的裁定电梯操作员。

游戏围绕路线判断、系统记录、监控证据、乘客证词、延迟关门和日志展开。玩家不控制整栋大楼，只能暂时改变系统对一段路线的理解方式。

## 仓库根目录

仓库根目录应包含以下内容：

- `project.godot`
- `AGENTS.md`
- `PROJECT_STATE.md`
- `assets/`
- `data/`
- `docs/`
- `scenes/`
- `scripts/`

不要在嵌套的重复目录中工作，例如：

- `the_shaft/the_shaft/`

不要在仓库内创建第二个 Godot 项目。

除非明确要求，否则不要移动 `project.godot`。

## Godot 版本规则

- 本项目使用 Godot 4.7。
- 使用与 Godot 4.7 兼容的 GDScript。
- 不要使用 Godot 3.x API。
- 如果 Godot 4.7 有更清晰的实现方式，不要沿用旧版 Godot 4.x 的行为假设。
- 优先使用简单、易读的 GDScript，而非花哨的引擎技巧。
- 场景、脚本和资源必须能在 Godot 4.7 中打开，且不出现转换警告。
- 保持项目对 Godot 初学者友好。

## 演示目标

构建一个 10–15 分钟的垂直切片，验证以下核心循环：

1. 出现一条调度。
2. 一名乘客进入电梯。
3. 玩家查看系统记录。
4. 玩家查看监控证据。
5. 玩家询问或观察。
6. 玩家选择路线理由。
7. 电梯到达。
8. 玩家填写日志。
9. 世界在之后给出间接反馈。

首个演示应证明游戏的体验与氛围，而非展示世界的完整规模。

## 当前演示范围

首个可玩的演示应聚焦于：

- 一个电梯操作舱。
- 少量乘客事件。
- 基础调度流程。
- 基础路线选择。
- 基础日志选择。
- 占位或简化的监控证据。
- 必要时使用占位美术。
- 最少但可读的 UI。
- 清晰的开发结构。

在核心循环可用前，不要实现大型系统。

避免加入：

- 可自由漫游的大楼探索。
- 大型地图系统。
- 完整的 3D 角色控制器。
- 复杂的物品栏系统。
- 大型分支任务框架。
- 完整的存档/读档系统。
- 程序化生成。
- 完整的关系系统。
- 完整的音频中间件。
- 未经要求的插件。

## 核心设计规则

- 不要把游戏变成可自由漫游的大楼探索游戏。
- 不要把大楼塑造成简单的邪恶反派。
- 系统建议可以是合理的。
- 违背系统并不总是正确。
- 稳定性有时应当保护人。
- 偏离流程有时也会伤害人。
- 玩家改变的是系统对路线的理解，而不只是要去哪个楼层。
- 小动作应当有意义：延迟关门、查看记录、切换监控、再多问一句、填写日志。
- 玩家应感觉自己是运行中系统里的工作人员，而不是外部反抗者。
- 每个重要行动都应带来责任，而非简单的英雄主义。
- 游戏应避免“系统坏、反抗好”这种显而易见的道德结构。

## 技术规则

- 使用 Godot 4.7。
- 除非明确要求，否则使用 GDScript。
- 保持场景小巧且可组合。
- 优先采用数据驱动的乘客事件。
- 不要在 UI 脚本中硬编码乘客事件文本。
- 不要添加大型第三方依赖。
- 除非同步更新所有引用，否则不要重命名已有公开 API。
- 不要创建不必要的全局单例。
- 不要重新格式化整个项目。
- 不要修改无关文件。
- 针对当前 issue 做出最小且有用的改动。
- 优先使用清晰的名称，而不是简短的名称。
- 优先使用明确的状态流程，而不是隐藏的魔法逻辑。
- 保持脚本对 Godot 初学者易读。

## 版本控制规则

- 在当前 Issue 的独立 feature branch / 仓库外 worktree 中执行已冻结、通过风险门的计划。
- 默认分支名：`dev/<issue-number>-<short-name>`。不得在无关功能分支上直接开发。
- 允许在该分支创建关联 Issue 的本地 commit；提交前检查 diff 和暂存文件，只纳入本 Issue 改动。
- 不自主 push、merge 或 force-push；不未经授权 rebase/reset 改写共享历史、删除远端分支或修改权限。
- 保留用户未提交改动，不自动 stash/reset；无法安全隔离时停止并报告。
- worktree 必须位于仓库外的获准路径；记录 base commit 和尚未合入 main 的依赖。
- 除非当前 issue 有要求，否则不要修改 `.gitignore`、`.gitattributes` 或仓库设置。
- 不要将生成的缓存目录加入版本控制。
- 不要将 `.godot/` 加入版本控制。
- 不要将导出的构建产物加入版本控制。
- 不要将临时文件、日志或编辑器备份加入版本控制。
- 将每次改动限定在当前 GitHub issue 范围内。
- 修改文件后，准确说明改动了哪些文件，以及如何测试。

## VS Code / Codex 工作流程规则

- Codex 通常在 VS Code 中使用。
- 将包含 `project.godot` 的文件夹视为仓库根目录。
- 开始工作前读取此文件、`PROJECT_STATE.md`、当前 Issue 正文及有效评论，并检查实际仓库。
- 只处理当前 GitHub issue。
- 不要实现额外系统。
- 不要重构或重新设计整个项目。
- 不要修改无关文件。
- 不要重新格式化整个项目。
- 按版本控制规则仅创建范围内本地 commit，停在 push / merge 前。
- 编码后，请总结：
  - 改动的文件。
  - 实现的内容。
  - 如何在 Godot 4.7 中测试。
  - 已知限制。

## 文件夹结构规则

使用以下总体结构：

- `assets/art/`：精灵图、纹理、UI 美术和视觉资源。
- `assets/audio/`：音乐、环境音和音效。
- `data/passengers/`：乘客事件数据。
- `data/routes/`：路线数据。
- `docs/`：设计说明和实现说明。
- `scenes/main/`：主入口场景。
- `scenes/elevator/`：电梯舱及相关场景。
- `scenes/passenger/`：乘客场景。
- `scenes/ui/`：UI 面板。
- `scripts/core/`：共享工具和核心定义。
- `scripts/flow/`：演示流程与状态管理。
- `scripts/passenger/`：乘客数据与行为。
- `scripts/route/`：路线选项与路线逻辑。
- `scripts/ui/`：UI 脚本。
- `scripts/camera/`：摄像头切换与摄像头效果。

除非当前 issue 需要，否则不要新建顶层文件夹。

## 场景规则

- 主入口场景应放在 `scenes/main/`。
- UI 面板应放在 `scenes/ui/`。
- 保持每个场景的职责单一。
- 不要把全部游戏逻辑放进一个庞大的场景脚本中。
- 可以数据驱动时，不要在场景节点中直接硬编码乘客内容。
- 优先通过清晰的信号或显式方法调用将 UI 连接到流程管理器。
- 避免无关场景之间的隐藏依赖。
- 保持节点名称清晰且稳定。

## GDScript 风格规则

- 使用清晰的类名和变量名。
- 在能提升可读性时使用类型标注。
- 避免炫技式的一行写法。
- 避免过深的逻辑嵌套。
- 注释应说明意图，而非显而易见的语法。
- 为重要逻辑使用简洁的中文注释，帮助 Godot 初学者理解代码。
- 尽可能保持函数简短。
- 演示流程优先采用简单的状态机。
- 早期原型任务中输出有用的调试信息。
- 在演示确实需要前，不要引入复杂架构。

## 中文注释规则

- 所有重要代码都应保留适量的中文注释。
- 中文注释应帮助 Godot 初学者理解项目结构和逻辑。
- 状态机、输入处理、信号连接、UI 节点引用、数据结构以及关键路线、乘客和日志逻辑，都应有简短的中文注释。
- 注释应说明逻辑负责什么，以及为何如此实现。
- 不要逐行添加注释。
- 不要把显而易见的代码逐字翻译成中文注释。
- 保持注释简洁，不要把它们写成长篇设计文档。

## 数据驱动的乘客规则

乘客事件最终应以数据表示，而不是硬编码在 UI 脚本中。

一个乘客事件可包含：

- `case_id`
- `display_name`
- `route_grade`
- `system_request`
- `passenger_request`
- `record_lines`
- `camera_clues`
- `route_options`
- `log_options`
- `consequences`

早期原型可以使用简单的占位数据，但需保持结构便于后续替换为真实资源。

## 路线设计规则

路线系统不是大型交通模拟系统。

玩家不应手动规划每一段电梯路径。

玩家选择的是让大楼如何理解这次移动。

路线选项可包括：

- 标准送达。
- 延迟送达。
- 照护路线。
- 维护绕行。
- 等待换乘。
- 返回路线。
- 未登记停靠。
- 手动覆盖。
- 报告异常。

关键问题不仅是“乘客要去哪里”，更是“系统将把这次移动记录为什么”。

## UI 规则

操作员控制台应像工作界面，而不是奇幻风格菜单。

重要的 UI 模块可包括：

- 调度面板。
- 乘客记录面板。
- 监控面板。
- 路线选项面板。
- 门控面板。
- 通信面板。
- 日志面板。
- 稳定性或系统状态面板。

早期 UI 可以简单甚至粗糙，但必须可读、可测试。

在核心循环可用前，不要花时间打磨最终视觉效果。

## 摄像头规则

摄像头系统是游戏的核心。

摄像头应提供证据，而不只是装饰。

可用的摄像头视角包括：

- 正面摄像头。
- 侧面摄像头。
- 地面摄像头。
- 货舱摄像头。
- 门口摄像头。
- 顶角摄像头。

不同摄像头应揭示不同信息：

- 面部、姿势、衣物、徽章、说话状态。
- 双手、隐藏物件、侧身姿态。
- 鞋子、脚印、液体、阴影。
- 包、推车、违禁物品、补给物资。
- 门口切面、走廊状态、等候的人影。

早期实现可以使用文本占位符。

后续摄像头视觉可采用：

- 低帧率。
- 扫描线。
- 时间戳。
- 延迟。
- 噪点。
- 压缩伪影。
- 失真。
- 识别框。

玩家应感觉自己看到的是系统压缩后的影像，而不是完整的人。

## 美术方向规则

- 使用低成本的纸片人 / 广告牌风格乘客。
- 除非明确要求，否则不要用完整 3D 角色替代这种风格。
- 普通乘客可以保持简单。
- 关键乘客可以拥有更多朝向和特殊姿势。
- 乘客清晰度比动画流畅度更重要。
- 摄像头必须提供不同证据，而不只是不同视角。
- 监控视觉可以通过风格化掩盖低成本美术，但也必须表达世界观。
- 玩家是通过大楼的记录系统看见人，而不是通过直接的人际接触。

## 叙事规则

- 乘客事件应描写人如何被流程部分地误读。
- 避免把乘客写成抽象的受害者。
- 避免把每次偏离流程都写成英雄行为。
- 避免把每条标准路线都写成残酷行为。
- 大楼使用日常的管理语言：
  - 建议
  - 路线
  - 风险
  - 维护
  - 许可
  - 标准流程
  - 非必要访问
  - 稳定性
  - 记录
  - 调度
- 对话应克制而务实。
- 角色不应直接解释整个主题。
- 乘客应像正在设法抵达某处的人，而不是讲解设定的符号。
- 玩家应经常面对不完整的信息。
- 好的乘客事件应让玩家思考：“流程足以描述这个人吗？”

## 大楼呈现规则

大楼不是简单的反派。

大楼不应说“服从我”。

大楼不应像愤怒的独裁者一样行动。

大楼应让人感到它是：

- 基础设施
- 物流
- 住房
- 维护
- 运输
- 风险控制
- 许可
- 记录
- 工作流程
- 稳定性管理

大楼可以同时是冰冷、合理、有用、保护人、有害、盲目且不完整的。

它的恐怖来自日常流程语言造成难以承受的结果。

## 选择设计规则

好的选择不应有显而易见的道德答案。

稳定性有时必须确实有用。

偏离流程有时必须确实有代价。

玩家不应学会如下简单规则：

- “永远服从系统。”
- “永远违背系统。”
- “永远相信乘客。”
- “永远相信记录。”

每个事件都应取决于具体证据、具体的人和具体后果。

## 日志系统规则

日志不只是计分界面。

日志决定系统如何记住这一事件。

可用的日志操作包括：

- 记录为标准送达。
- 记录乘客自述。
- 标记轻微异常。
- 报告路线冲突。
- 隐藏非关键偏离。
- 记录延误。
- 不添加额外说明。

日志应让人感到责任。

避免把日志做成简单的好/坏评分。

## 门控规则

门的操作应有意义。

重要的门控操作可包括：

- 开门。
- 关门。
- 延迟关闭。
- 重新打开。
- 拒绝进入。
- 在目的地保持开启。
- 允许短暂的舱外行动。
- 锁定。

延迟关门是项目中最重要的操作之一。

它可以意味着：“我愿意为这个人再等几秒。”

## 音频方向规则

游戏的音乐和声音应烘托氛围，而非压倒玩家。

推荐的声音方向：

- 氛围吉他。
- 后摇滚。
- 噪声环境。
- 低沉的机械嗡鸣。
- 电梯电机。
- 继电器咔嗒声。
- 柔和的 UI 提示音。
- 远处的广播。
- 室内底噪。
- 私密时刻的短吉他句。

避免：

- 过于英雄主义的音乐。
- 过于煽情的情绪戏剧化处理。
- 赛博朋克夜店节拍。
- 到处使用通用恐怖音效。
- 持续的大音量音乐。

## Issue 实现规则

用户输入 `Run issue #<number>` 时，读取
[`the-shaft-issue-runner`](docs/development/the-shaft-issue-runner.md) 并按当前阶段续接。
此入口是项目工作模板，依赖已授权的 GitHub 连接和本地工具，不是无人值守服务。

### 单一事实源

- GitHub Issue 正文及有效评论是动态任务唯一真相源：冻结规格、PLAN、审批、BUILD、REVIEW、分支/提交/PR、待验收事项。
- `PROJECT_STATE.md` 只记录慢变化事实：版本、入口、结构、已实现系统、冻结决策、已知技术债和禁止重复创建项。仅当实际结构变化时更新；不记录当前 Issue、任务进度或分支状态。
- 本文件只约束 Agent 行为和项目设计边界，不保存具体任务内容。
- 实际场景、脚本和 `project.godot` 是可执行事实；与 Issue 或文档冲突时报告差异，不猜测设计意图。

### 四阶段协议

1. **DESIGN**：需求未冻结时只读、分析、提出方案和待决问题。不得自行冻结玩法/视觉或因方案合理就改代码。用户选定方案或 Issue 已有明确冻结规格后结束。
2. **PLAN**：读取规则、稳定事实、Issue 正文/评论、Git 状态和相关代码/场景。列明目标、范围、非目标、风险及理由、预计文件/场景、实际基线、Godot 人工操作、测试方案、验收标准、冲突。不能只重排 Issue。
3. **BUILD**：仅执行冻结且通过风险门的计划，一次一个 Issue，隔离分支/worktree，最小改动。遇到范围外故障记录并报告，不顺手修复。
4. **REVIEW**：BUILD 后自动执行统一检查并审查 diff。报告实际文件、实现、各测试/Validator 结果、已知限制、计划偏差、Godot 体验检查点、base/branch/commit/PR 和待验收项；回写当前 Issue，停在 push / merge 前。

### 风险门

| 等级 | 判断依据 | 行为 |
| --- | --- | --- |
| GREEN | 文档、测试、校验器或不影响结构/API的明确局部修复，边界清楚且可验证 | 冻结后可直接 BUILD，仍须测试和 REVIEW |
| AMBER | Scene Tree、节点/信号、公共 API、UI 重组、Input/Camera/Interaction 接线、多运行时关系 | 先 PLAN，等待用户或 ChatGPT 对具体计划的批准后 BUILD |
| RED | 大量删除、数据迁移、大规模重构、权限/历史改写、核心玩法替换等 | 人工主导，每一步明确授权；不自主执行 |

风险按实际影响判断，不能只看文件扩展名，也不能自行下调 Issue 已冻结的等级。
有效审批必须指向计划版本、范围和约束；同一计划已有批准则不重复询问。
最新有效修订优先；超出批准范围重新过风险门。`Run issue` 本身不替代 AMBER / RED 审批。
该协议是 Agent 工作约定，不能冒充系统权限层的强制隔离。

### 工程检查与交接

- 使用 `powershell.exe -NoProfile -File .\tests\run_all_tests.ps1` 运行统一检查；Godot 路径可用 `-GodotPath` 或 `GODOT_BIN` 指定。
- 若系统禁止执行脚本，按 tests/README.md 在获准后仅对测试进程使用 `-ExecutionPolicy Bypass`，不修改系统策略。
- 统一检查包括版本/项目根、编辑器解析、GDScript/Scene Contract、已有测试和真实主场景 headless smoke。
- 同一失败根因最多自动重试 2 次；第 3 次出现时立即停止并报告。先运行最小复现，单个阶段失败时不得从头循环全量检查。
- 重复权限请求、重复命令或测试阶段重入视为 Circuit Breaker 触发条件；未经新批准不得改用更多变体继续尝试。
- 失败、未执行、自动通过、用户验收、Git 提交、GitHub 回写必须分别记录；不得把 headless 通过当成视觉验收。
- Scene Contract 仅检查代码依赖和公共接口，不冻结装饰层级、美术参数，不通过修改正式内容让测试通过。
- GitHub 回写前读取现有评论以防重复；仅追加/更新本 Agent 对应交接区块，保留用户内容。回写失败如实报告，不伪造远端 commit/PR。
- 用户负责玩法、美术、范围冻结、关键设计决策、Godot 体验验收及 push / merge 决定。

## 测试规则

每次修改代码时，都要说明如何测试。

好的测试说明应包括：

- 应打开哪个场景。
- 应按哪个按钮（如有）。
- Output 面板中应出现什么。
- 屏幕上应看到什么。
- 不应发生什么。

对于早期原型任务，简单地打印 Output 调试信息是可以接受的。

如果项目无法在 Godot 4.7 中打开，不要宣称任务完成。

## 架构事实

当前入口、主要场景、运行时职责和已实现系统以 `PROJECT_STATE.md` 和实际仓库为准。
不要根据过往原型里程碑重建旧入口或另起状态机。

## 最后提醒

《The Shaft》不是关于逃离大楼。

它讲的是：为何这栋大楼难以拒绝。

玩家不在系统之外。

玩家正是让系统显得正常的人之一。

每个实现决策都应守护这一核心。

## 开发日志与飞书同步规则

- 以 GitHub Issue 为单位记录开发日志；没有 Issue 编号时，应明确标记为“未关联 Issue”，不得自行猜测编号。
- 开发日志必须区分：代码实现、Godot 运行、用户验收、Git 提交和飞书同步。未发生的事项应如实标记为“未执行”或“未知”。
- 当用户说“整理开发日志”时，只能生成或更新本地日志草稿，不得写入飞书或其他外部服务。
- 只有当用户明确说“同步开发日志”时，才允许将对应的本地日志写入飞书，并删除本地草稿。
- 日志整理本身不发起 commit 或 push；日志只记录已有、可验证的提交。工程实现的本地 commit 遵循版本控制规则。
- 不允许凭推测、计划或未验证的输出宣称功能已完成、Godot 已运行或用户已验收。
- 不允许将密钥、Token、App Secret、Cookie 或其他凭据写入仓库、开发日志或同步状态文件。
- 飞书同步实现必须与具体连接方式解耦；在未配置并获得用户明确授权前，不创建飞书应用、不安装第三方飞书 MCP，也不请求凭据。
- 已授权的开发日志同步目标为飞书 Wiki「开发日志」：`https://tcn5hkroyoex.feishu.cn/wiki/FwaVwW1Rniy4iEkJycgckZ3unK1`。仅在用户明确要求“同步开发日志”后，才可向该文档追加对应 Issue 的日志区块。

## 飞书 CLI 环境

本项目已配置飞书 CLI。

Windows 环境调用：

lark-cli.cmd

不要使用：
lark-cli.ps1

原因：
PowerShell 执行策略可能阻止 ps1 包装器。

飞书 CLI 用途：
- 读取 The Shaft 开发日志文档
- 更新指定开发日志文档
- 搜索相关飞书资料

调用原则：
- 默认只读；
- 用户明确输入“同步开发日志”后才允许写入；
- 写入前必须读取目标文档确认结构；
- 禁止覆盖整篇文档；
- 禁止删除已有人工内容；
- 不保存 Token、Secret 或登录信息到仓库。
